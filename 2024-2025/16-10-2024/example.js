// alert("Hello JS!"); Показывает сообщение

let x = 10; // с помощью оператора let объявляются переменные
const y = 20; // с помощью оператора const создаются константы

// Вот таким образом создаются объекты
const persona = {
    name: "Harry Potter"
}

const cityName = "Saint-P"; // Строчные типы данных

const isBigCity = true; // Булевы типы данных

console.log(10 + 100);

console.log(x + y);

console.log(cityName, isBigCity);


